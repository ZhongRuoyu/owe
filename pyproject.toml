[project]
name = "iou"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "flask>=3.1.3",
  "flask-cors>=6.0.2",
  "gunicorn>=26.0.0",
]

[dependency-groups]
dev = ["ruff>=0.15.14", "ty>=0.0.40"]
